The folder contains new P1 version, for 128KB RAM / 512KB RAM systems

It improves the old P1 version, allowing processing of larger C files.

The "" file contains the error/warning codes produced by P1 while processing a file that has syntax problems.
