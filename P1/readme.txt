The folder contains the enhanced P1 version, for 128KB RAM / 512KB RAM systems

It improves the old P1 version, allowing processing of larger C files.

The "P1 error and warning messages.txt" file contains the error/warning codes + description, produced by P1 while processing a file that has syntax problems.
